FullscreenToggle_DataTableVehicleLeader_Weekly: function () {
	// console.log("FullscreenToggle_DataTableVehicle_Daily");

    $('body').on('click', '#btn-fullscreen-datatable-weekly', function () {
        
        if ($.fn.DataTable.isDataTable('#datatableVehicleLeader_Weekly') ) {
            datatableVehicleLeader_Weekly.columns.adjust().draw();
        }

        if($(this).attr('data-click-state') == 1) {
            $(this).attr('data-click-state', 0);
            // console.log("btn-fullscreen-datatable-daily, state-00, exit fullscreen");

        } else {
            $(this).attr('data-click-state', 1)
            // console.log("btn-fullscreen-datatable-daily, state-01, goto fullscreen");
        }
     
    });

  
},