FullscreenToggle_DataTableVehicle_Quaterly: function () {
	// console.log("FullscreenToggle_DataTableVehicle_Quaterly");

    $('body').on('click', '#btn-fullscreen-datatable-quaterly', function () {
        
        if ($.fn.DataTable.isDataTable('#datatableVehicle_Quaterly') ) { 
            datatableVehicle_Quaterly.columns.adjust().draw();
        }

        if($(this).attr('data-click-state') == 1) {
            $(this).attr('data-click-state', 0);
            // console.log("btn-fullscreen-datatable-weekly, state-00, exit fullscreen");

        } else {
            $(this).attr('data-click-state', 1)
            // console.log("btn-fullscreen-datatable-weekly, state-01, goto fullscreen");
        }
     
    });

  
},

