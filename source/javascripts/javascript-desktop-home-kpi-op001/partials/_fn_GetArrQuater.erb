GetArrQuater: function (data) {
	// console.log("GetArrQuater");



    // console.log(data);
    // console.log(data.rows);
    // console.log(data.rows[0]);
    // console.log(data.rows[0].details);

    var quaterArray = [];

    // console.log(data.rows[0]);
    // console.log(data.rows[0].details.length);

    $.each( data.rows[0].details, function( i, val ) {
             quaterArray.push(val.quarter_name + "/" + val.year_no);
    });

    return quaterArray;

  
},

