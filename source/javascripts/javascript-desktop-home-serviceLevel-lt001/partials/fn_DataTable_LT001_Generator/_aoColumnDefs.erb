            "aoColumnDefs": [
                    {
                        'bSortable': true,
                        "bAutoWidth": true,
                        'aTargets': [0]
                    },{
                        // The `data` parameter refers to the data for the cell (defined by the
                        // `data` option, which defaults to the column being worked with, in
                        // this case `data: 0`.
                        "render": function ( data, type, row ) {
                            // console.log(row);
                            //return '<input type="checkbox" />';
                            return row.section;
                            // return '-';
                        },
                        "mData": "section",
                        "targets": 0
                    },{
                        "render": function ( data, type, row ) {
                            return row.bill_all;
                        },
                        "mData": "bill_all",
                        "targets": 1
                    },{
                        "render": function ( data, type, row ) {
                            return row.bill_in_commitment;
                        },
                        "mData": "bill_in_commitment",
                        "targets": 2
                    },{
                        "render": function ( data, type, row ) {
                            // return row.total_percentage;
                            return numeral(row.total_percentage).format('0,0.00') + " %";
                        },
                        "mData": "total_percentage",
                        "targets": 3
                    },{
                        "render": function ( data, type, row ) {
                            return row.customer_error;
                        },
                        "mData": "customer_error",
                        "targets": 4
                    },{
                        "render": function ( data, type, row ) {
                            // return row.iel_percentage;
                            return numeral(row.iel_percentage).format('0,0.00') + " %";
                        },
                        "mData": "iel_percentage",
                        "targets": 5
                    },{
                        "render": function ( data, type, row ) {
                            return row.non_branch_error;
                        },
                        "mData": "non_branch_error",
                        "targets": 6
                    },{
                        "render": function ( data, type, row ) {
                            // return row.dc_percentage;
                            return numeral(row.dc_percentage).format('0,0.00') + " %";
                        },
                        "mData": "dc_percentage",
                        "targets": 7
                    }
            ],