    <script id="datatable-monthly-header-template" type="text/x-handlebars-template">

        <thead>
            
            <tr>
                {{#each headers}}

                {{#if_eq monthHeader 'ลำดับ'}}
                    <th class="" rowspan="2">{{monthHeader}}</th>
                {{else if_eq monthHeader 'ศูนย์'}}
                    <th class="" rowspan="2">{{monthHeader}}</th>
                {{else}}
                    <th class="DatatableVehicle-common" colspan="2">{{monthHeader}}</th>
                {{/if_eq}}
                
                {{/each}}
            </tr>
            <tr>
                {{#each headers}}

                {{#if_eq monthHeader 'ลำดับ'}}
                   <!--  <th class="" ></th> -->
                {{else if_eq monthHeader 'ศูนย์'}}
                    <!-- <th class="" ></th> -->
                {{else}}
                    <th>Drop</th>
                    <th>Box</th>
                {{/if_eq}}


                {{/each}}
            </tr>
            
        </thead>
        <tbody>
        </tbody>
        

    </script>