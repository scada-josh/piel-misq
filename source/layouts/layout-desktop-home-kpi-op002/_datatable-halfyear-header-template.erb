    <script id="datatable-halfyear-header-template" type="text/x-handlebars-template">

        <thead>
            
            <tr>
                {{#each headers}}

                {{#if_eq halfyearHeader 'ลำดับ'}}
                    <th class="" rowspan="2">{{halfyearHeader}}</th>
                {{else if_eq halfyearHeader 'ชื่อหัวหน้าสาย'}}
                    <th class="" rowspan="2">{{halfyearHeader}}</th>
                {{else}}
                    <th class="DatatableVehicle-common" colspan="2">{{halfyearHeader}}</th>
                {{/if_eq}}
                
                {{/each}}
            </tr>
            <tr>
                {{#each headers}}

                {{#if_eq halfyearHeader 'ลำดับ'}}
                   <!--  <th class="" ></th> -->
                {{else if_eq halfyearHeader 'ชื่อหัวหน้าสาย'}}
                    <!-- <th class="" ></th> -->
                {{else}}
                    <th>Drop</th>
                    <th>Box</th>
                {{/if_eq}}


                {{/each}}
            </tr>
            
        </thead>
        <tbody>
        </tbody>
        

    </script>